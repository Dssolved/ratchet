# Состояние работ

**Обновлять в конце каждого рабочего блока.** Этот файл — точка входа для новой сессии:
из него должно быть понятно, где мы остановились, без археологии по коду.

Последнее обновление: 2026-08-15

---

## Сейчас

Шаги 0 и 1 закрыты. Приложением уже можно пользоваться: провести тренировку, записать
подходы, посмотреть и поправить историю.

Дизайн-система зафиксирована до начала вёрстки: направление «Спорт», только тёмная тема,
токены в `src/index.css`, правила в [design.md](design.md).

**Следующий шаг — шаг 2** (Android и APK). Он не добавляет фич: смысл в том, чтобы поймать
сюрпризы сборки на маленьком проекте и проверить, доходит ли локальное уведомление
при потушенном экране — от этого зависит шаг 3.

## План

Реализовывать строго по порядку, не перескакивая.

### Шаг 0 — каркас · ✅ готово

- Vite 8 + React 19 + TS 7 + Tailwind v4, версии из [../CLAUDE.md](../CLAUDE.md#стек)
- Zustand store + persist в IndexedDB (`idb-keyval`), хук `useHydrated`
- Типы в `src/domain/types.ts`, конвейер миграций в `src/store/migrations.ts`
- Работа с локальными датами и ISO-неделями — `src/domain/dates.ts`
- Seed из [progressions.md](progressions.md): 6 семейств, шаблоны A/B/Свободная
- Экспорт и импорт JSON с проверкой формата и снимком перед заменой
- `npm run check` (typecheck + oxlint) и `npm run build` проходят чисто

Что проверено вручную в браузере:

- seed рендерится, стартовые ступени и диапазоны совпадают с документом
- persist пишет в IndexedDB только данные, экшены не утекают
- подменённое хранилище подхватывается при перезагрузке (гидратация работает)
- сброс к seed создаёт снимок перед перезаписью
- импорт: валидный файл применяется; не-JSON, чужой JSON, будущая версия схемы и бэкап
  без движений дают понятные ошибки и **не разрушают текущие данные**

Правки модели, всплывшие при реализации: `unit` переехал на ступень (Д-14), версия схемы
вынесена в обёртку (Д-15).

### Шаг 1 — логирование тренировки · ⬜ следующий

- Три вкладки: «Сегодня», «Прогресс» (пока только история), «Настройки»
- Выбор дня со следующим по очереди шаблоном, свободная тренировка, `+ движение` на ходу
- Экран тренировки: предзаполнение прошлым результатом, степперы, отмена подхода тапом,
  автораскрытие следующего движения
- `perSide` (строки «1 Л» / «1 П»), `unit: 'seconds'` (шаг степпера 5), `kind: 'binary'`
  («получилось» / «пробовал»)
- Завершение с экраном итога, история с правкой и удалением задним числом
- Схема поднята до v2: у `Workout` появился `movementIds`

Что проверено вручную в браузере:

- полная тренировка «День A» — 15 подходов, автопереход сработал на каждом упражнении,
  ни одного действия навигации за тренировку;
- секунды планки попали в итог отдельно от повторений (98 повторений без неё);
- **предзаполнение**: вторая тренировка подставила 9 · 9 · 8 из первой, по номерам подходов;
- предложенный день сменился с A на B после завершения A;
- отмена подхода тапом по отмеченной строке, отмена всей тренировки;
- правка задним числом (9 → 11) и удаление подхода в истории;
- **миграция v1 → v2 на реальных данных**: план восстановлен из подходов в порядке
  появления, для тренировки без подходов — из её шаблона, журнал не пострадал.

### Шаг 2 — Android и APK · ⬜ следующий

Рано и специально: сборка Android — единственное место с неприятными сюрпризами,
и ловить их надо на маленьком проекте. См. [android.md](android.md).

- Установка окружения, `npx cap add android`, первая сборка, APK на телефоне
- Проверить главное: доходит ли локальное уведомление при потушенном экране

### Шаг 3 — таймер отдыха · ⬜

- Плашка отсчёта, `+30 сек`, пропуск
- `@capacitor/local-notifications` — уведомление при свёрнутом приложении
- `@capacitor/haptics` — вибрация на отметке подхода и на конце отдыха

### Шаг 4 — храповик · ⬜

- Расчёт готовности к усложнению (правило в [data-model.md](data-model.md))
- Индикаторы на списке движений
- Карточка «Ступень взята», переход, запись `StepChange`
- Откат на ступень вниз, `maxReachedStepOrder`

### Шаг 5 — прогресс · ⬜

- Лестница ступеней (визуальное ядро)
- График с полосами ступеней и метками переходов
- Экран итога тренировки
- Блок «Всего» — суммарные повторения с фильтром по периоду

### Шаг 6 — стрик и ачивки · ⬜

- Недельный стрик, цель тренировок в неделю
- Ачивки: рекорд в подходе, взятая ступень, вехи по суммарным повторениям

### После MVP

Карта задействованных мышц (SVG-силуэт, проверить лицензию — на GitHub в основном CC-BY),
напоминания по застоявшимся навыкам, виджет на домашний экран.
Список отвергнутого — в [spec.md](spec.md#что-сознательно-не-делаем).

## Открытые вопросы

- Точные требования Capacitor 8 к JDK и Android SDK — уточнить на шаге 2 при первой сборке
  и записать в [android.md](android.md).
- Диапазоны и стартовые ступени в [progressions.md](progressions.md) — гипотеза. Проверяются
  первыми тренировками, правятся из вкладки «Настройки». Особенно ступень 5 брусьев:
  падение с 3×15 до 3×7–8 ожидаемо, но величину шага увидим на практике.
